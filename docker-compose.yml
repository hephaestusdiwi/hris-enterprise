services:
  php:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: hris_php
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - ./backend:/var/www/html
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_DATABASE: ${DB_DATABASE:-hris}
      DB_USERNAME: ${DB_USERNAME:-hris}
      DB_PASSWORD: ${DB_PASSWORD:-secret}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:-secret}
    depends_on:
      - postgres
      - redis
    networks:
      - hris_network

  nginx:
    build:
      context: ./docker/nginx
      dockerfile: Dockerfile
    container_name: hris_nginx
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8000}:80"
    volumes:
      - ./backend:/var/www/html
    depends_on:
      - php
    networks:
      - hris_network

  postgres:
    image: postgres:17-alpine
    container_name: hris_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_DATABASE:-hris}
      POSTGRES_USER: ${DB_USERNAME:-hris}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-secret}
    command: >
      postgres
      -c max_connections=50
      -c shared_buffers=128MB
      -c work_mem=4MB
      -c maintenance_work_mem=64MB
      -c effective_cache_size=256MB
      -c log_min_duration_statement=1000
    volumes:
      - hris_postgres_data:/var/lib/postgresql/data
      - ./docker/postgres/init:/docker-entrypoint-initdb.d:ro
    ports:
      - "${DB_PORT:-5432}:5432"
    networks:
      - hris_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-hris} -d ${DB_DATABASE:-hris}"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: hris_redis
    restart: unless-stopped
    command: redis-server /usr/local/etc/redis/redis.conf --requirepass ${REDIS_PASSWORD:-secret}
    volumes:
      - hris_redis_data:/data
      - ./docker/redis/redis.conf:/usr/local/etc/redis/redis.conf:ro
    ports:
      - "${REDIS_PORT:-6379}:6379"
    networks:
      - hris_network
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD:-secret}", "--no-auth-warning", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

  node:
    image: node:22-alpine
    container_name: hris_node
    user: "${HOST_UID:-1000}:${HOST_GID:-1000}"
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - ./frontend:/var/www/html
    ports:
      - "${VITE_PORT:-5173}:5173"
    command: >
      sh -c "if [ -f package.json ]; then npm install && npm run dev -- --host 0.0.0.0; else echo 'frontend belum di-install (lihat STEP 12)'; tail -f /dev/null; fi"
    networks:
      - hris_network

  queue:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: hris_queue
    restart: unless-stopped
    working_dir: /var/www/html
    stop_grace_period: 30s
    volumes:
      - ./backend:/var/www/html
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_DATABASE: ${DB_DATABASE:-hris}
      DB_USERNAME: ${DB_USERNAME:-hris}
      DB_PASSWORD: ${DB_PASSWORD:-secret}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:-secret}
    command: ["/usr/bin/supervisord", "-c", "/etc/supervisord.conf"]
    depends_on:
      - php
      - postgres
      - redis
    networks:
      - hris_network

  scheduler:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: hris_scheduler
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - ./backend:/var/www/html
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_DATABASE: ${DB_DATABASE:-hris}
      DB_USERNAME: ${DB_USERNAME:-hris}
      DB_PASSWORD: ${DB_PASSWORD:-secret}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:-secret}
    command: >
      sh -c "if [ -f artisan ]; then while true; do php artisan schedule:run --verbose --no-interaction >> storage/logs/scheduler.log 2>&1; sleep $(( 60 - $(date +%s) % 60 )); done; else echo 'backend belum di-install (lihat STEP 11)'; tail -f /dev/null; fi"
    depends_on:
      - php
      - postgres
      - redis
    networks:
      - hris_network

networks:
  hris_network:
    driver: bridge

volumes:
  hris_postgres_data:
  hris_redis_data:
